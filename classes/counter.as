// Recycle It Game// counter.as// (C)2011 Jam Zhangpackage {		import flash.display.Sprite;	import com.greensock.*;	import com.greensock.easing.*;		public class counter extends Sprite {				public static const SCORE_DURATION:Number=.5;		public static const SCORE_SCALE:Number=1.25;		public var previousCount:int=0;		public var currentCount:int=0;		public var digits:Array=[];				public function counter():void {			digits[0]=d1;			digits[1]=d2;			digits[2]=d3;			update();		}				public function add(inc:int=1):void {			previousCount=currentCount;			currentCount=Math.max(currentCount+inc,0);			update();		}				public function update(count:Object=null):void {			if(count!=null) {				currentCount=int(count);			}			var s:String=String(currentCount);			while (s.length<3) {				s='0'+s;			}			s=s.substr(s.length-3,3);						for(var n:int=0;n<3;n++) {				if(digits[n]!=undefined) {					digits[n].gotoAndStop(int(s.charAt(n))+1);				}			}						if(previousCount!=currentCount) {				scaleX=scaleY=SCORE_SCALE;				TweenLite.to(this, SCORE_DURATION, {scaleX:1, scaleY:1, ease:Sine.easeOut});				}		}			}}