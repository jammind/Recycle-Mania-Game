// Recycle It Game// banner.as// (C)2011 Jam Zhangpackage  {		import flash.display.MovieClip;	import flash.display.Sprite;	import com.greensock.*;	import com.greensock.easing.*;	import flash.events.MouseEvent;			public class banner extends Sprite {				const DURATION:Number=.25;				public function banner():void {			addEventListener(MouseEvent.MOUSE_OVER,onMouseEvent);			addEventListener(MouseEvent.MOUSE_OUT,onMouseEvent);			addEventListener(MouseEvent.CLICK,onMouseEvent);		}				function onMouseEvent(e:MouseEvent):void {			switch(e.type) {								case MouseEvent.MOUSE_OVER:					sfxPlayer.playSound('rollover');					TweenLite.to(inner, DURATION, {y:20, ease:Sine.easeOut});						break;								case MouseEvent.MOUSE_OUT:					TweenLite.to(inner, DURATION, {y:0, ease:Sine.easeOut});						break;								case MouseEvent.CLICK:					sfxPlayer.playSound('click');					sfxPlayer.playSound('stopTictac');					(root as MovieClip).gotoAndPlay('forceExit');					break;							}		}			}	}